# Dependencies / build
node_modules/
dist/

# OS / editor cruft
.DS_Store
.thumbnail
Thumbs.db

# Local captures
renders/
test-results/
playwright-report/

# Secrets — local only, NEVER commit. (The .example template IS tracked.)
secrets.local.json
.env
.env.local
.env.*.local
